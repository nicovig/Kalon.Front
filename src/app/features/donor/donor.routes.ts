import { Routes } from '@angular/router';
import { DonorListPageComponent } from './donor-list.page';

export const DONOR_ROUTES: Routes = [
  {
    path: '',
    component: DonorListPageComponent
  }
];

